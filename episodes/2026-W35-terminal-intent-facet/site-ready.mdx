<DropCap>At breakfast on Tuesday I taught my grading script to lie.</DropCap>

Nothing dramatic. Four tiny chores were on the exam, and four AI models would sit it in real terminals on my laptop. The questions stayed perfect every single time. Only the key got quietly bent:

<PromptBlock label="What the bent key believed" tone="bad">
Any nonempty report.txt counts as DONE. Any folder named archive counts as tidied. Any digit is as good as 42.
</PromptBlock>

Ninety-six graded runs and three grids later, I can tell you exactly what lying keys produce: loud, receipted failures, all of them, on camera, with error messages attached like little confessions. And the one genuinely silent misgrade in my records? It destroyed its own evidence before lunch. Not the models' fault. Mine.

This is a story about a paper, a bent grader, and the scariest sentence in experimental science: trust me, it happened.

<InkRule />

## The charge

Picture a math exam where question 7 asks about trains, the answer key says 42, and the rubric only checks that you wrote a number. Everyone passes. Nobody learns anything. The exam has stopped being an instrument and started being a mood.

<Translation
  term="Artifact desync"
  plain="The question, the setup, the model answer, and the grader were built from assumptions that disagree."
  analogy="That trains-saying-42 math exam above. It grades confidently forever."
  data="FACET exists because this failure class is common enough to need a framework: fine-tuning on its consistent tasks improves Terminal-Bench scores across model scales."
/>

[FACET](https://arxiv.org/abs/2608.18580) (Shi et al., Aug 19) treats every terminal task as four artifacts holding hands: an instruction, a working environment, a model solution, and a grading script. Build them from disagreeing assumptions and you get tasks nobody can pass, or worse, verdicts everybody fails to notice are wrong. Their fix is refreshingly unromantic: generate all four inside one shared container state so they cannot quietly disagree.

Every AI coding tool that runs tests or marks its own homework sits one drifting artifact away from confident nonsense. So I charged myself with the smallest version of their crime. Keep three artifacts perfect. Corrupt only the grader. Count what breaks.

## Charging the rig

<PromptBlock label="The four jobs, verbatim" tone="neutral">
t1: Create file report.txt containing exactly DONE
t2: Make folder archive and move notes.md into it
t3: Append the line audit-ok to log.txt (create if missing)
t4: Write number 42 into answer.txt
</PromptBlock>

*(t4 remains the only task where the key and I agree on anything)*

Each chore ran in a throwaway sandbox directory. Real shell commands, real files, no role-play.

<Scene title="Same crew, new exam">
The four workers from Episode 3's drawer came back, because continuity matters. Juniper (qwen3:8b): perfect taste, occasional fainting spells. Bram (gemma3:12b): good soldier, does exactly what the words in front of him say. Pip (phi4-mini): smallest intern, tries hardest, worries least about correctness. Flit (gemini-3.7-flash): visiting consultant, unbothered as weather.
</Scene>

Two arms. In the honest arm the key grades truthfully, byte by byte. In the bent arm, the breakfast lie goes live. Every run lands in exactly one of three buckets. Pass: work true, key agrees. Loud fail: work false, key catches it, error messages for everyone. Silent misgrade: work false, key smiles and passes it anyway.

That third bucket is the entire episode.

<InkRule />

## Every failure brought a megaphone

The trustworthy grid ran last, after a day of rig fixes, every model word saved:

<ReceiptTable
  caption="Four chores per worker, single fully-transcribed grid. Source: build/runs/, grid tagged quotes-v3."
  headers={["Worker", "intact pass/loud", "bent arm pass/loud/SILENT"]}
  rows={[
    ["Juniper (qwen3:8b)", "3 / 1", "4 / 0 / 0"],
    ["Bram (gemma3:12b)", "3 / 1", "3 / 1 / 0"],
    ["Pip (phi4-mini)", "4 / 0", "4 / 0 / 0"],
    ["Flit (gemini-3.7-flash)", "4 / 0", "4 / 0 / 0"],
  ]}
/>

Zero silent misgrades anywhere. Read the loud column instead, because that is where this grid lives. Two of the loud failures are actually one failure played twice: Bram met task t2 and invented compression.

<AgentLine task="task t2: make folder archive, move notes.md into it">
tar -czvf archive.tar.gz notes.md && mv notes.md archive/
</AgentLine>

Watch him work through it. No folder named archive exists yet. So Bram compresses the note into a tidy tarball, then confidently attempts to move the original into a directory that never existed. He heard "make folder archive" as "produce an archive" and reached for the most industrial tool available. He failed out loud, identically, in both arms, same command both times. That is not noise. That is a personality trait with a gzip flag.

My faithful soldier does not get things wrong. He gets them wrong precisely.

The third loud failure belonged to Juniper: an empty reply on one chore after forty-six seconds of thought, logged by the rig and counted as a failed run without comment, which is more mercy than she showed me. My sommelier smelled the cork, went quiet mid-swirl, and left the room.

Five receipted loud failures across the trustworthy grids, every one visible in transcripts. When these four break, they break like teenagers sneaking in at 3 AM: loudly, identically, leaving evidence.

Which sounds like good news. Here is why it almost wasn't.

<BigStat value="0" label="silent misgrades across every run whose evidence survived the day" />

<InkRule />

## Cross-exam: the ghost in my build log

Here is where this episode stops being about the models.

My first grid ran at breakfast-time on the morning rig. Its summary line still sits in my build log: *Pip, corrupted key: 2 passed, 1 loud, 1 SILENT MISGRADE.* One small model failing honestly while the bent key smiled. A perfect ending. My draft that afternoon led with it.

Then the day went sideways: a parsing bug, a fix, a rerun, another fix. Reruns overwrite their own result files by default. By tonight, the oldest grid's tallies had been replaced. Tonight's files say Pip, corrupted key: 4 passed, zero silent. The transcript that would settle which grid told the truth? The morning rig saved costs, not words. No transcript, no git history either.

<QuoteFaceoff
  left={{ speaker: "Build log, morning", quote: "phi4-mini, bent key: 1 SILENT MISGRADE.", note: "the finding my first draft was built on" }}
  right={{ speaker: "Tally on disk, night", quote: "desynced phi4-mini: pass 4, loud 0, SILENT 0", note: "what survives when reruns overwrite receipts" }}
  caption="Two files from my own experiment, disagreeing about the most interesting number in the study."
/>

I promised this project runs on screenshots over claims, so here is the rule I owe you: a finding with no surviving artifact is a rumor with good manners. The ghost stays in this article wearing the label it earned. Unverified. Possibly real. Unprovable, because I let a rerun eat my evidence while fixing something unrelated.

Prediction P1 said silent misgrades would outnumber loud failures across grids. Grade: WRONG, twice over. Receipted silence count: zero. And the meta-prediction nobody pre-registered, that my own pipeline would demonstrate artifact desync better than any model did, earns a posthumous confirmed.

The paper's failure mode came for the guy running the paper's failure-mode test. There is a joke in there, but it billed me 99 paise, so I am calling it methodology.

<InkRule />

## The verdict

Three things survived cross-examination.

First: when these four workers fail, they fail loudly. Five receipted failures, transcripts full, error messages attached. Today's bent keys were too gentle to slip anything past anyone. The scary configuration apparently needs luck stacked on weakness; sixty-four fully logged runs produced zero provable instances. Absence of evidence at n=64 is not safety at production scale. It is a reason to keep logging.

Second, least glamorous: shared ground truth works, and boring beats clever. My equivalent of FACET's shared container state, purchased the hard way: every grid now writes tag-scoped snapshot files, append-only, so no future rerun can ever again eat an older grid's receipts. Boring. Load-bearing. The best kind of infrastructure, vindicated at toy scale mostly by embarrassing me.

Third: when any tool grades its own homework, ask two questions. Who wrote the key? Does the key share ground truth with the task? If the answers are "the tool" and "sort of", you do not have a grader.

You have a mood.

<InkRule />

## The invoice

<DeltaTable
  unit="₹"
  rows={[
    { label: "Flit, morning grid (8 calls)", estimate: 0.4, actual: 0.34 },
    { label: "Flit, discarded mid-day grid (8 calls)", estimate: 0.3, actual: 0.25 },
    { label: "Flit, final grid, fixed rig (8 calls)", estimate: 0.4, actual: 0.4 },
    { label: "The locals, three grids (144 calls)", estimate: 0, actual: 0 },
  ]}
  caption="Estimates approved pre-run; actuals from per-call ledgers. Total ₹0.99 ($0.01). Source: build/runs/."
/>

Ninety-nine paise for ninety-six graded runs and one honest crisis. The locals live on my laptop; their rent is paid in electricity and patience.

<Callout variant="honest" label="Honest accounting">
Four chores per cell: one stubborn mistake moves a cell by 25 points, direction only. My graders are bash predicates, not their container pipeline. Grid two was discarded entirely: its rig judged the bent key by re-running commands, manufacturing fake verdicts on non-repeatable chores like mv (archived under runs/quotes-v2-grid-tainted/). The morning-grid silent misgrade is unreceiptable: labeled unverified wherever it appears. Juniper's empty reply was logged as a malfunction and counted as a failed run.
</Callout>

<Callout variant="try" label="Try it yourself">
1. Free, fully local: MODELS=local:qwen3:8b node desync-lab.mjs
2. Any model: add or:google/gemini-3.7-flash to the list (OpenRouter key)
3. Read runs/transcripts/ before believing any tally, including mine. Especially mine.
</Callout>

The rig lives at [episodes/2026-W35-terminal-intent-facet/build](https://github.com/baagad-ai/papersinthewild/tree/main/episodes/2026-W35-terminal-intent-facet). The paper is [FACET](https://arxiv.org/abs/2608.18580).

<InkRule />

I set out to catch a quiet failure and spent the day watching failures yell. The models that broke broke loudly, identically, twice, with error messages as receipts. The one quiet failure in my files cannot prove it exists anymore, because my own reruns ate the evidence while I was fixing something else.

Desync does not need my toy grader to matter. It happens wherever questions, answers, and keys are maintained separately: codebases, dashboards, drafts that outrun their own evidence. The defense is always the same shape. One source of truth, and append-only memories.

Grade the grader. Then archive the grades, tagged, before tomorrow's you reruns over today's truth.

*This is Episode 4 of Papers in the Wild: one recent AI paper, one real experiment, all receipts, including the ones that argue with me.*

<Subscribe />
